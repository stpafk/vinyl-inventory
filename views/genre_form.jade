extends layout

block content
  h1 #{title}

  form(method='POST' action='')
    div.form-group
      label(for='name') Genre:
      input#name.form-control(type='text', placeholder='Rock, Pop etc.' name='name' value=(undefined===genre ? '' : genre.name))
      label(for='description') Description: 
      label(for="description") Description:
      textarea#description.form-control(type='text', placeholder='Description', name='description', value=(undefined === artist ? '' : genre.description))
    button.btn.btn-primary(type='submit') Submit

  if errors
   ul
    for error in errors
     li!= error.msg