extends layout

block content
  h1= title

  ul
    each vinyl in vinyls
      li
        a(href=vinyl.url) #{vinyl.vinyl_name}
        |  (#{vinyl.artist.artist_name})

    else
      li There are no vinyls.
